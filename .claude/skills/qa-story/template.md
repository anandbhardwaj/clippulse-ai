# QA block template

Fill every `<...>`, delete rows or sections that do not apply (never leave placeholders), keep plain ASCII. The block must start and end with the two marker comments; `scripts/upsert_qa.py` replaces everything between them on re-runs.

```markdown
<!-- qa:start -->
---

## QA - Testing and Coverage Status

| | |
|---|---|
| **Verdict** | <PASS / PASS WITH NOTES / FAIL / BLOCKED / NOT READY> - <one-line reason> |
| **QA date** | <YYYY-MM-DD> |
| **Code under test** | `<short sha>` on `<branch>` (<committed or uncommitted working tree in WHERE>) |
| **Board status seen** | <status name> |
| **Board recommendation** | <what a human should do next; not performed by QA> |
| **Run by** | /qa-story, independent of the implementation; numbers below were re-run, not copied |

### Automated checks

| Check | Result | Command |
|---|---|---|
| Story tests | <N passed, N failed, N skipped> (`<test files>`) | `uv run pytest <files> -q` |
| Whole suite | <N passed, N failed> | `uv run pytest tests -q` |
| Hermetic run | <N passed with empty API keys and a dead HTTP proxy, or not run> | `<KEY>= HTTP_PROXY=http://127.0.0.1:9 HTTPS_PROXY=http://127.0.0.1:9 uv run pytest tests -q` |
| Lint | <clean or N errors> | `uv run ruff check .` |
| Types | <clean or N errors> | `uv run mypy src` |

### Coverage

| Module | Statements | Missed | Branches | Partial | Combined | Missing lines |
|---|---|---|---|---|---|---|
| `src/<module>.py` | <n> | <n> (<pct>% covered) | <n> | <n> | <pct>% | <ranges> |

Measured with `uv run --with pytest-cov pytest <files> --cov=src.<module> --cov-branch`.

### Acceptance criteria traceability

| AC | Scenario | Tests | Status | Notes |
|---|---|---|---|---|
| AC1 | <title> | `<test names>` | COVERED | <what was asserted; gaps if PARTIAL; deviation if any> |

<X of Y> acceptance criteria COVERED, <n> PARTIAL, <n> MANUAL, <n> NOT COVERED.
Tests not mapped to an AC: <none | list>.

### Untested code

| Location | What is not exercised | Related AC | Severity |
|---|---|---|---|
| `src/<module>.py:<lines>` | <description> | AC<k> | <Low or Info> |

### Definition of Done

| Item | Status | Evidence |
|---|---|---|
| <item text> | <Verified or Not verified or Needs a human> | <evidence or reason> |

### Findings

| # | Severity | Finding | Evidence | Suggested action |
|---|---|---|---|---|
| F1 | <High or Medium or Low or Info> | <one sentence> | <file:line or test name> | <action> |

### Not proven by automation

- <e.g. all external services and tools are faked; real behaviour unverified>
- <e.g. no cross-story integration test>

<sub>Generated by /qa-story. Re-running replaces this block; text outside the QA markers is never edited.</sub>
<!-- qa:end -->
```
